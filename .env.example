# ============================================
# StorySeal Environment Variables Template
# ============================================
# Copy this file to .env.local and fill in your actual values
# 
# On Windows PowerShell:
#   Copy-Item env.template .env.local
#
# On Linux/Mac:
#   cp env.template .env.local
#
# Then edit .env.local and replace placeholder values with your actual API keys

# ============================================
# Story Protocol Configuration (Required)
# ============================================

# Story Protocol RPC URL
# Option 1: Public RPC (RECOMMENDED for ABV.dev auto-registration)
# This matches the RPC that ABV.dev uses for Story Protocol integration
# Use this if you want ABV.dev to auto-register IP assets
NEXT_PUBLIC_STORY_RPC_URL=https://aeneid.storyrpc.io

# Option 2: Tenderly RPC (Optional - for advanced debugging & development)
# Only use this if you need Tenderly debugging tools and don't use ABV.dev auto-registration
# Get your Tenderly RPC URL from: https://dashboard.tenderly.co
# Steps: Register → Create Organization → Contact Support → Get RPC URL
# Format: https://rpc.tenderly.co/fork/{fork-id}
# NOTE: If using Tenderly RPC, ABV.dev auto-registration may not work (RPC mismatch)
# NEXT_PUBLIC_STORY_RPC_URL=https://rpc.tenderly.co/fork/your-fork-id-here

# ============================================
# ABV.dev API (Required for AI Image Generation)
# ============================================
# Get your API key from: https://app.abv.dev
# Sign up and get your API key from the dashboard
NEXT_PUBLIC_ABV_API_KEY=your-abv-api-key-here
NEXT_PUBLIC_ABV_API_URL=https://app.abv.dev

# ============================================
# Pinata IPFS (Optional - for IPFS Storage)
# ============================================
# Required if you want to upload metadata to IPFS
# Get credentials from: https://pinata.cloud
# 
# Option 1: API Key + Secret Key (Recommended)
NEXT_PUBLIC_PINATA_API_KEY=your-pinata-api-key
NEXT_PUBLIC_PINATA_SECRET_KEY=your-pinata-secret-key

# Option 2: JWT Token (Alternative to API Key + Secret Key)
# NEXT_PUBLIC_PINATA_JWT_TOKEN=your-pinata-jwt-token

# ============================================
# Reverse Image Search APIs (Optional)
# ============================================
# These are used for reverse image search feature in Monitor page
# You can configure these from Settings page in the dashboard
# 
# SerpAPI - Get from: https://serpapi.com (100 free searches/month)
# SERPAPI_API_KEY=your-serpapi-api-key

# Serpdog - Get from: https://serpdog.io (Free tier available)
# SERPDOG_API_KEY=your-serpdog-api-key

# Bing Visual Search - Get from: https://azure.microsoft.com (Free tier via Azure)
# BING_VISUAL_SEARCH_API_KEY=your-bing-api-key

# Google Custom Search - Get from: https://developers.google.com/custom-search
# GOOGLE_CUSTOM_SEARCH_API_KEY=your-google-api-key
# GOOGLE_CUSTOM_SEARCH_ENGINE_ID=your-search-engine-id

# TinEye - Get from: https://tineye.com
# TINEYE_API_KEY=your-tineye-api-key

# ============================================
# WalletConnect (Optional)
# ============================================
# Get your project ID from: https://cloud.walletconnect.com
# If not set, only MetaMask and injected wallet will be available
# This enables WalletConnect protocol for connecting wallets
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your-walletconnect-project-id

# ============================================
# Tenderly Configuration (Optional - Advanced Debugging)
# ============================================
# Get from Tenderly Dashboard → Settings → API Keys
# Only needed if you want to use Tenderly API for advanced features
# TENDERLY_ACCESS_KEY=your-tenderly-access-key
# TENDERLY_PROJECT_SLUG=your-project-slug
# TENDERLY_ORG_SLUG=your-org-slug

# ============================================
# SPG NFT Contract (Optional)
# ============================================
# Default testnet contract will be used if not provided
# Usually not needed - SDK will handle automatically
# NEXT_PUBLIC_SPG_NFT_CONTRACT=0x...

# ============================================
# Wallet Private Key (Optional - Server-side Only)
# ============================================
# WARNING: Never commit this to version control!
# Only needed for server-side operations (currently not used)
# WALLET_PRIVATE_KEY=0x...

# ============================================
# Important Notes:
# ============================================
# 1. All NEXT_PUBLIC_* variables are exposed to the browser
#    - Use only for non-sensitive configuration
#    - API keys in NEXT_PUBLIC_* are visible in client-side code
#
# 2. Never commit .env.local to git (already in .gitignore)
#    - This file contains your actual API keys
#    - Only commit env.template (this file)
#
# 3. You can also configure these settings from the Settings page in the dashboard
#    - After connecting your wallet, go to Dashboard → Settings
#    - API keys stored in Settings page are saved in localStorage (client-side only)
#
# 4. For ABV.dev auto-registration:
#    - Use Public RPC (default above) to match ABV.dev's RPC
#    - Tenderly RPC may cause RPC mismatch issues
#
# 5. For advanced debugging:
#    - Use Tenderly RPC for better transaction debugging
#    - Note: ABV.dev auto-registration may not work with Tenderly RPC
#
# 6. If you switch RPC URL:
#    - Clear localStorage: localStorage.removeItem('story_rpc_url')
#    - Or update from Settings page
#
# 7. Testnet Tokens (IP Tokens):
#    - Story Protocol Aeneid Testnet uses IP tokens (not ETH)
#    - Get from: https://docs.story.foundation/aeneid
#    - Or: https://faucet.unitynodes.com
#    - Chain ID: 1315
#
# 8. Priority for API Keys:
#    - Settings page (localStorage) > Environment variables
#    - This allows users to override env vars from the UI
